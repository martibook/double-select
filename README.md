# double-select

A browser extension: Select a context text as A, and select a part of it as B, ask LLM to explain B given the context of A.

## Setup

Make a copy of `sample.config.json` and name it as `config.json`, fill in valid values for keys in it.